# Overview
This full-stack web application is a comprehensive consultation platform connecting consultants and clients, primarily leveraging an AI assistant for personalized financial insights. Its purpose is to enhance interactions, streamline financial guidance, and improve client outcomes and consultant efficiency. The platform includes a multi-tier subscription system for AI agents with revenue sharing, an employee licensing system, and a public landing page for lead generation, aiming to be a holistic solution for modern financial consultation.

# User Preferences
Preferred communication style: Simple, everyday language.
User requested "obsessive-compulsive" attention to detail when verifying what works and what doesn't.
User prefers direct SQL for any database modifications (not ORM migrations).

# System Architecture
The application features a modern UI/UX built with React 18, TypeScript, Vite, Tailwind CSS, and `shadcn/ui`, prioritizing accessibility and responsiveness. The backend uses Express.js, TypeScript, JWT, bcrypt, and PostgreSQL (Drizzle ORM), implementing robust role-based access control (consultant, client, super_admin) and a multi-profile system. The UI/UX employs a Revolut-style design system with reusable CSS utilities and shared components, ensuring responsive and accessible UI with proper dark mode support using CSS variables.

**Key Features and Design Patterns:**

*   **AI Integration:** Provides personalized financial insights, multi-tenant semantic search, consultation summarization, and AI-powered integrations (WhatsApp Business, Instagram, X DMs). Includes configurable AI sales agents, automated follow-up and client check-ins, and tools for generating courses and exercises. A ChatGPT-style AI Assistant offers dynamic model selection, reasoning visualization, conversation memory, and a hybrid AI context system with token optimization and RAG. A Consultation Function Calling System with an LLM Intent Classifier ensures intelligent tool usage and guardrails. Knowledge Base enhancement allows custom instructions in AI system prompts.
*   **Subscription & Licensing:** A multi-tier subscription system manages AI agent access, consultant licenses, revenue sharing, and AI credits with Stripe integration.
*   **Content & Marketing Studio:** Features AI tools for content idea generation, social media copy, a 6-step campaign builder, AI image generation, and a Lead Nurturing 365 System with automated AI-generated email sequences. A Content Autopilot System automates content scheduling. **Brand Voice "Voce & Stile Personale" section** (5th collapsible in `BrandVoiceSection.tsx`): personalTone, contentPersonality, audienceLanguage, avoidPatterns, writingExamples (max 3 real post samples), signaturePhrases (catchphrase array). All fields stored in `content_studio_config.brand_voice_data` JSONB. **audienceSegments** field: array of `{name, description}` stored in `brand_voice_data` JSONB, editable both in Brand Voice (Authority & Posizionamento section) and inline in Content Studio Step 1 via "+" button (saves via `/api/content/brand-voice` POST) with "×" to remove. Segments appear as selectable radio-style rows in "Chi vuoi raggiungere" — clicking fills Nicchia/Pubblico Target, clicking again clears. **"Consiglia Inserzione"** section in Step 1: user writes an ad idea, clicks "Migliora con AI" → backend `POST /api/content/ai/improve-ad` returns improved version with hook, angle, improvements list, tips. "Usa come contesto" appends to additionalContext, clears the form (usa-e-getta), and saves to in-session history with "Riusa" option. Server-side: `buildBrandVoiceContext()` in `content-ai-service.ts` injects all fields into AI prompt with structured sections. Conditional `writingExamplesPriorityInstruction` activates when examples are present, instructing AI to replicate the consultant's exact style. Enhanced `universalRule` with advanced anti-robot rules (syntax variety, natural transitions, concrete examples, varied openings, simple lexicon, intentional imperfections). **AdVisage PRO** uses `gemini-3.1-flash-image-preview` (Nano Banana 2) for ad image generation via provider factory (server-side, no client API keys). Resolution: 2K default, 4K optional. Thinking level: High. Response modalities: IMAGE only. Supports 9 aspect ratios (1:1, 2:3, 3:2, 3:4, 4:3, 4:5, 5:4, 9:16, 16:9, 21:9). **Full customization system** in sidebar Factory Styles: mood (5 options), art style (5), lighting style (5), color grading (6), camera angle (6), background style (5), image quality (2K/4K), brand color picker. All settings are mapped to Italian photographic descriptions in `PROMPT_MAPPINGS` (shared between client `types.ts` and server `advisage-server-service.ts`). Prompts include: original ad copy text as "CONTESTO INSERZIONE", mood/style/lighting/color/camera/background sections, negative instructions (anti-AI-artifacts), human rendering rules, text rendering safeguards, and quality anchoring ("Nike/Apple/Audi level"). **AI Auto-style mode**: "Manuale / AI Auto" toggle in Factory Styles sidebar. When Auto, AI analyzes ad text and auto-selects optimal mood, style, lighting, color grading, camera angle, background, and image format. `recommendedSettings` object returned in analysis JSON. Client validates values against allowed enums before applying. Manual settings are cached when switching to Auto and restored when switching back. `stylesMode` persisted to localStorage. **Prompt preview**: collapsible section in concept card shows exact prompt before generating. **Original text**: shown in concept card with expand/collapse. Server function `buildCommonSections()` generates shared prompt sections used by both `buildPromptFromVisualJSON` and `buildLegacyImagePrompt`. **Session Persistence**: Batch productions are auto-saved to PostgreSQL via `advisage_sessions` table (id, consultant_id, name, settings jsonb, batch_results jsonb, post_inputs jsonb, status, timestamps). Generated images are saved as files in `uploads/advisage/{sessionId}/` and tracked in `advisage_generated_images` table (session_id FK, concept_id, variant, image_path, settings_used jsonb). API endpoints: GET/POST/PUT/DELETE `/api/content/advisage/sessions`, POST `/api/content/advisage/sessions/:id/images`. Frontend: "Storico" button in top bar opens session history dialog with load/delete. Sessions auto-created on batch analysis, images auto-saved on generation.
    *   **Millie Email Writer (Email Hub):** Branded as "Millie", the Email Hub (`/consultant/email-hub`) is an intelligent email management system with IMAP/SMTP, unified inboxes, and AI-powered response generation. Key features:
        *   **No-Reply Filter**: `shouldSkipAutoResponse()` detects and ignores test SMTP, bounce, newsletter, auto-reply, OOO emails before AI processing (processingStatus = 'ignored').
        *   **CRM Context Enrichment**: `buildContactContext()` searches lead_scraper_results, proactive_leads, and users to identify the sender and inject full CRM context into AI prompts. Enriched with Hunter context via `resolveHunterContext()` for proactive_lead matches.
        *   **Profilo Commerciale (Per-Account Sales Context)**: Each email account has its own `sales_context` jsonb column (9 fields: servicesOffered, targetAudience, valueProposition, pricingInfo, competitiveAdvantages, idealClientProfile, salesApproach, caseStudies, additionalContext). Configured in the AI Settings dialog (`EmailAISettings.tsx`). Injected directly into Millie's prompt with `[MILLIE-SALES]` logging. Completely independent from Hunter's `lead_scraper_sales_context`.
        *   **Client FileSearch Integration**: When the sender is a registered client, Millie searches their private FileSearch documents via `fileSearchService.getStoreNamesForGeneration()`. Logged with `[MILLIE-FILESEARCH]` prefix. Non-clients are skipped.
        *   **Cross-Channel Context**: `buildCrossChannelContext()` retrieves WhatsApp messages (8 msgs with contact name resolved via proactive_leads/users JOIN, message_type), voice call transcripts (3 calls with direction detection via caller_id/called_number), bidirectional email thread (15 emails with snippets), AI scheduled tasks for the contact (5 recent), and Hunter outreach actions (5 recent). Max context: 3000 chars. Injected into AI prompts.
        *   **Email Type Classification**: Deterministic `determineEmailType()` classifies emails as hunter_reply, system_notification, client_inquiry, lead_inquiry, or unknown. Stored in `hub_emails.email_type` column. Frontend shows color-coded badges and filter dropdown.
        *   **Adaptive Response Engine**: `detectResponseIntent()` identifies 7 intent types (info request, call accept, not interested, callback later, client problem, enthusiastic, generic) and applies scenario-specific response strategies.
        *   **Smart Non-Response Rules**: `detectSmartSkip()` avoids responding to acknowledgment-only emails, unsubscribe requests, and own outbound messages.
        *   **Millie Auto-Actions**: `executeMillieActions()` post-processes AI drafts to update lead status (interested/lost), create reminder tasks, cancel follow-ups, and escalate uncertain emails.
        *   **Outreach Pipeline**: A dedicated tab showing the complete Hunter email outreach flow — emails sent, follow-ups scheduled, responses received — with stats and quick actions. Backend endpoint: `GET /api/email-hub/outreach-pipeline`. Filters for Hunter-only emails via `email_type IN ('hunter_outreach', 'hunter_followup')` tag on `hub_emails` + fallback to lead_email matching from `ai_scheduled_tasks`. Shows enriched lead data: AI score, contacted channels (voice/WA/email), phone, website. Stats: totalSent, totalReplied, totalInSequence, totalCompleted, totalNoResponse, followUpsInQueue, avgScore.
        *   **Sent Email Tracking**: Compose and Reply endpoints save outbound emails to `hub_emails` with `folder: "sent"`, `direction: "outbound"`, and `receivedAt` set to current time. Inbox ordering uses `COALESCE(received_at, sent_at, created_at)` for robustness. List API computes `toEmail` from `toRecipients[0]`. Frontend shows recipient (not sender) for outbound emails with "A:" prefix.
        *   **Millie Branding**: The entire UI is branded as "Millie Email Writer" with avatar, personalized labels, and context visibility panels.
        *   **Ticket Email Notifications**: When Millie creates a ticket (low confidence, high urgency, negative sentiment, escalation keyword, no KB answer), an HTML email notification is sent to the consultant via the account's SMTP. Built in `sendTicketEmailNotification()` in `ticket-webhook-service.ts`. All dynamic content is HTML-escaped for security.
    *   **Hunter Email Templates Library**: `server/ai/email-templates-library.ts` — 10 structured persuasive email templates (first contact, follow-up, break-up, trigger event, case study, pain point, free value, referral, compliment, re-engagement) with `selectTemplateForScenario()` selection logic and `GOLDEN_RULES` copywriting guidelines. Integrated into `hunter-direct-executor.ts` for AI-guided email generation.
    *   **Automatic Email Follow-up System**: `checkEmailFollowUps()` in `ai-task-scheduler.ts` — cron job (09:00, 14:00, 18:00) checks outbound Hunter emails for replies. Configurable from the Lead Scraper page (Configurazione Hunter → Follow-up tab → "Follow-up Email Automatici" section). Config stored in `outreach_config.emailFollowUp` (jsonb on `ai_autonomy_settings`): `followUp1Days` (default 3), `followUp2Days` (default 7), `maxFollowUps` (default 2), `followUp1TemplateId`/`followUp2TemplateId` (select from 10 templates), `autoApprove` (default false — tasks go to waiting_approval), `enabled` toggle. Anti-duplication via result_data matching. Manual trigger: `POST /api/ai-autonomy/hunter/check-followups` (also reads config from DB). Interface: `EmailFollowUpConfig` in `shared/schema.ts`.
    *   **7-Phase Market Research Framework ("Ricerca di Mercato"):** Replaces the simple pain-point list with a comprehensive 7-phase accordion UI in `ideas.tsx`. Phases: Trasformazione (current/ideal state), Avatar del Cliente (8-field psychological profile), Leve Motivazionali (8 emotional driver checkboxes), Obiezioni e Resistenze (3 sub-groups), Errore Nascosto (core lies with importance sliders), Meccanismo Unico (name + description), Posizionamento (UVP formula). **Deep Research endpoint**: `POST /api/content/ai/generate-market-research` uses dual Google search (SerpApi via `searchGoogleWeb` + Gemini grounding with `tools:[{googleSearch:{}}]`) in parallel, then Gemini analysis to fill all 7 phases. Supports per-phase generation via `phase` param. Data stored as `MarketResearchData` interface in `shared/schema.ts`, persisted in `content_idea_templates.market_research_data` JSONB column. Emotional driver canonical keys: `sopravvivenza, piacere, fuga_dolore, sessualita, comfort, status, protezione, approvazione` (normalized from AI output). Full research data injected into AI idea generation prompt via `content-ai-service.ts` with structured sections. Backward compat with old `marketResearchProblems` array maintained.
*   **Data Analysis:** A "compute-first" system processes structured data, separating deterministic calculation (SQL) from AI interpretation. Includes efficient Excel parsing, dynamic tables with Row Level Security (RLS), Server-Sent Events (SSE), and an enterprise-grade Semantic Layer. A Multi-CSV Join System supports uploading multiple CSV files with automatic relationship detection.
*   **Consultant Workflow:** A 4-phase, 27-step Consultant Setup Wizard guides configuration, alongside configurable monthly consultation limits. An integrated **Accademia di Formazione** LMS at `/consultant/academy` provides dynamically managed lessons organized in modules with progress tracking. A **Client Library** (`/client/library`) uses an Academy-style layout for document and exercise presentation.
*   **Communication & Automation:**
    *   **Voice Telephony (Alessia AI Phone):** Integrates FreeSWITCH with Gemini 2.5 Flash Native Audio for real-time voice AI, automatic caller recognition, call management, and recording. **Call Recording**: VPS bridge starts `uuid_record` on call start, saves WAV to `/tmp/alessia_recordings/`. VPS bridge serves files via `GET /recordings/:filename` (service token auth). Main server saves `recording_url` at call end. Proxy endpoint `GET /api/voice/recording/:callId` authenticates user via JWT and streams from VPS bridge. Frontend audio players use proxy URL. **Overflow Message Delay**: `overflow_message_delay_secs` column on `voice_numbers` (default 15, range 5-60). VPS bridge passes `overflow_message_delay_ms` channel var to FreeSWITCH. Dialplan adds `sleep` between messages A-E so callers hear music pauses between announcements (like professional call centers). Configurable from UI "Pausa tra messaggi in coda" control. Includes a Direction-Based Voice Template System and AI Task Scheduling for automated calls. **Configurable Retry Rules**: Per-consultant toggle for each retry trigger (no_answer, busy, short_call, voicemail) stored in `consultant_availability_settings` (5 boolean columns: `voice_retry_on_no_answer`, `voice_retry_on_busy`, `voice_retry_on_short_call`, `voice_retry_on_voicemail`, `voice_amd_enabled`). Callback handler builds `retryableStatuses` array dynamically from DB instead of hardcoded list. **Answering Machine Detection (AMD)**: When `voice_amd_enabled=true`, VPS bridge adds `execute_on_answer='amd'` to FreeSWITCH originate command. ESL client listens for `amd::resolve` CUSTOM event; on `machine` result, skips audio stream to Gemini, kills call, and sends `voicemail` status callback to Replit. Requires `mod_amd` on FreeSWITCH; falls back to `short_call` detection if unavailable. **Call Hold Handling**: VPS bridge `esl-client.ts` subscribes to CHANNEL_HOLD/CHANNEL_UNHOLD events from FreeSWITCH. On hold, the inactivity timeout is paused via `sessionManager.pauseInactivityTimeout()`. **Per-Number Inactivity Timeout**: `inactivity_timeout_secs` column on `voice_numbers` (default 60). Configured per-number from frontend (30-300 sec range). VPS fetches via `overflow-config` API and passes to `createSession()`. Timer resets ONLY on caller voice activity (VAD with RMS>200 threshold on PCM audio) — AI audio output does NOT reset it. Gemini reconnections do NOT pause/resume the timer. When timeout fires, `handleCallStop()` + `uuid_kill` properly hangs up the FreeSWITCH call. **handleCallStop uuid_kill**: All call terminations (replit_disconnected, timeout, websocket_closed) now send `uuid_kill` to FreeSWITCH to ensure the SIP call is terminated. Previously, only the timeout callback did uuid_kill; now handleCallStop does it for all reasons. **Gemini Zombie Killer caller WS close**: When Zombie Killer terminates a Gemini session (30min idle / 2h max), `forceCloseConnection()` also closes the `callerWebsocket` (VPS→Replit WS), which triggers `onClose` → `handleCallStop` → `uuid_kill`. **FreeSWITCH sched_hangup**: Both inbound (`01_alessia_9999.xml`) and outbound (`02_outbound_trunk.xml`) dialplans have `sched_hangup +3600` as absolute safety net. **Inbound Overflow on Server Rejection**: When VPS bridge passes an inbound call to Replit but Replit rejects it (CHANNELS FULL), the catch block now routes the call to the overflow queue (hold music + auto-return when slot opens) instead of killing it. Outbound calls still get `uuid_kill` on rejection (callback retry handles them). Overflow routing logic extracted into reusable `routeToOverflow()` function. **Outbound Hangup Callback System**: VPS bridge `esl-client.ts` intercepts CHANNEL_HANGUP events for outbound calls, maps FreeSWITCH hangup causes to status callbacks via `notifyOutboundCallResult()` in `caller-context.ts`. Mapping: NO_ANSWER/NO_USER_RESPONSE/RECOVERY_ON_TIMER_EXPIRE→`no_answer`, USER_BUSY/CALL_REJECTED→`busy`, NORMAL_CLEARING with effectiveDuration<15s→`short_call`. Guards: skips if AMD already detected, active Gemini session exists, or ORIGINATOR_CANCEL. Uses `effectiveDuration = billsec > 0 ? billsec : duration` for accuracy. **Attempts Log**: `scheduled_voice_calls.attempts_log` (JSONB array) populated by callback handler in all 3 branches (completed/failed/retryable) with `{attempt, status, hangup_cause, duration_seconds, timestamp, event}`. **Unified Contact Timeline**: `GET /api/voice/contact/:phone` returns `timeline[]` (voice_calls + scheduled_voice_calls + attempts_log entries + voice_call_events, sorted DESC) and `stats{}` (totalCalls, completed, failed, noAnswer, busy, shortCall, voicemail, totalDuration, avgDuration, firstContact, lastContact). **CRM Tab**: New tab in `consultant-voice-calls.tsx` with search/filter by contact type and last outcome, card-based contact list with inline stats, lazy-loaded expandable timeline per contact, color-coded event icons. **Enriched Contact Profile**: `consultant-voice-call-detail.tsx` enhanced with Timeline tab (first tab, vertical timeline with expandable call details), enriched stats header, attempts_log mini-timeline in Programmate tab, sidebar activity recap. **Configurable Backoff Mode**: `voice_retry_backoff_mode` (varchar: 'exponential' | 'manual') + `voice_retry_manual_delays` (jsonb array of minutes per attempt). In exponential mode: baseInterval × 2^attempt, cap 30min. In manual mode: user-defined delay per retry attempt (1-60 min each). UI shows mode selector (Esponenziale/Manuale), per-attempt input fields in manual mode, and live timeline preview for both modes. **Per-Number Max Concurrent Calls**: `max_concurrent_calls` is now per-number (stored in `voice_numbers.max_concurrent_calls`, default 5). Configured in the "Gestione Overflow" card per-number panel with an input (1-10). The Voice Bridge checks per-number limit BEFORE `createSession()` via `sessionManager.canAcceptCallForNumber()`. Global env var `MAX_CONCURRENT_CALLS` remains as safety cap. Overflow dequeue (`tryDequeueOverflow`) also respects per-number limits, re-inserting at queue front with preserved timeout when limits are full. **Inbound Overflow Queue**: When all concurrent AI lines are busy, inbound callers enter an overflow queue instead of being rejected. Per-number config in `voice_numbers` table: `overflow_enabled` (bool, default true), `overflow_timeout_secs` (int, default 120), `overflow_dtmf_enabled` (bool, default true — caller presses 1 to transfer to consultant), `overflow_auto_return` (bool, default true — auto-transfer back to AI when slot frees), `overflow_message` (text, custom TTS message). Voice Bridge `session-manager.ts` manages in-memory FIFO queue with timeout handles. FreeSWITCH dialplan `05_overflow_queue.xml` handles MOH + cyclic announcement + DTMF "1" binding for transfer to `fallback_number`. `tryDequeueOverflow()` called on session end transfers first queued caller back to AI via `uuid_transfer`. API: `PATCH /api/voice/numbers/:id/overflow` (save config), `GET /api/voice/overflow-config?called_number=X` (service endpoint for Voice Bridge). UI: "Gestione Overflow / Coda d'Attesa" card in VPS tab with switches, slider, input, textarea per number. CHANNEL_HANGUP cleanup in `esl-client.ts` removes disconnected callers from queue.
    *   **AI Autonomous Employee (Multi-Role):** A Decision Engine uses Gemini to analyze client context and generate multi-step execution plans with granular real-time activity logging. Guardrails enforce working hours and channel restrictions. Ten specialized AI agents exist (Alessia, Millie, Echo, Nova, Stella, Marco, Robert, Hunter, Leonardo/Architetto, Personalizza — Iris was removed as redundant with Millie), supporting per-role client filtering, dual reasoning modes, post-generation anti-duplication of tasks, configurable AI model selection (Gemini 3 Flash / 3.1 Pro) with thinking levels, and per-role temperature control. **Stale Task Cleanup**: `waiting_approval` tasks >72h no longer block clients; >7 days auto-expire. `in_progress` tasks >2h auto-fail. **Differentiated Cooldown**: Failed tasks (keywords: no_answer, voicemail, busy, timeout, etc.) have 4h cooldown vs 24h for successful. **Fair Client Rotation**: Clients ordered by least-recently-processed (LEFT JOIN on ai_scheduled_tasks) instead of alphabetical, eliminating bias for >50 client pools. The Dipendenti AI card list auto-sorts Marco first (when active), then other enabled employees, then disabled ones. **Spontaneous Telegram Messages**: During each 30-min task generation cycle, AI roles can autonomously send a Telegram message to the consultant using `[TELEGRAM_MESSAGE]...[/TELEGRAM_MESSAGE]` tags in their response. Messages are saved to `agent_chat_messages` for web chat history. Controlled by `telegram_spontaneous_enabled` boolean column on `ai_autonomy_settings` (default true), with a toggle in Step 1 "Configurazione Avanzata".
    *   **Hunter Cross-Channel Context Resolver:** `server/services/hunter-context-resolver.ts` — Centralized service that makes Hunter lead data (ai_sales_summary, ai_compatibility_score, category, website, business_name, teamMembers) persistent and available across all 3 channels. `resolveHunterContext()` performs cascade lookup: proactiveLeadId → phone → email → direct phone in `lead_scraper_results`. All 3 SQL queries include `website_data` column. `extractTeamMembers()` parses team members (name/role) from `website_data.teamMembers` JSON. Link chain: `proactive_leads.metadata->>'hunter_lead_id'` → `lead_scraper_results.id`. `formatHunterContextForPrompt()` produces consistent formatted block for AI system prompts including team member names/roles and smart name-handling rules: `smartBusinessName()` abbreviates long business names (>3 words) by filtering Italian stop-words and legal suffixes, with fallback for all-stopword names. Prompt instructs Alessia to use person names when available, abbreviated business name otherwise, never recite full long names. **Placeholder Name Filtering**: `filterPlaceholderName()` utility in `ai-task-scheduler.ts` and `gemini-live-ws-service.ts` filters out placeholder contact names ('Contatto', 'Cliente', 'Unknown', 'Sconosciuto', 'Lead', 'Lead Sconosciuto', ''). `ensureProactiveLead` also filters the same list before saving to DB. Integrated in: Voice (early-start parallel query, injected in both instruction and non-instruction prompt paths), WhatsApp (after proactive lead lookup, appended to system prompt), Email (enriches `buildContactContext()` proactive_lead matches). Ensures AI knows the lead's business context even months after first contact.
    *   **Email Pool Rotation (Full Propagation):** `pool_id` from `outreach_config` is now propagated across ALL 5 call sites of `scheduleIndividualOutreach()` — hunter-analyze-crm, hunter-single-lead ("Avvia Hunter"), hunter-plan/execute, `handleCrmLeadOutreach` (scheduler), and lead-scraper search+outreach batch. `pool_id` is stored in `additional_context` of each email task. `executeSingleEmail()` in `ai-task-scheduler.ts` uses pool rotation when `pool_id` is present: resets daily counters, picks account with fewest sends today, increments counter after send, saves to `hub_emails` with `is_outreach_email=true, outreach_pool_id`. Falls back to single `email_account_id` if no pool configured (backward-compatible). UI fix: lead scraper pool selector now shows single-account selector when pool_id is empty/null, additively instead of exclusively.
    *   **Hunter Outreach System (Individual Task Architecture):** Autonomous lead prospecting pipeline using SerpApi for Google Maps/Search and Firecrawl for enrichment. Hunter is the "commercial brain" — finds leads, qualifies them, decides channel/timing, then creates **individual AI-personalized tasks per lead** in the correct calendars. ALL outreach flows (manual "Analizza CRM", CRM Lead Outreach via `handleCrmLeadOutreach`, and Lead Scraper Search via `handleLeadQualifyAndAssign`) use the **same unified pipeline**: `generateOutreachContent()` (exported from `ai-autonomy-router.ts`) generates per-lead content (voice context, WA template-based messages, AI-written emails with template library), then `scheduleIndividualOutreach()` creates individual tasks — voice calls go into `scheduled_voice_calls` (Voice Calendar), WA messages into `ai_scheduled_tasks` with `task_type='single_whatsapp'` and `preferred_channel='whatsapp'` (WA Calendar, sent directly via Twilio without Decision Engine), emails via SMTP+`hub_emails` (Email Hub). **Multi-channel**: `handleLeadQualifyAndAssign` creates tasks for ALL available channels per lead (not just first priority match) — a lead with phone+email gets voice+WA+email tasks. Uses `selectBestPhone(forWhatsApp)` (prefers Italian mobile 3xx for WA) and `selectBestEmail` (filters generic prefixes like info@/admin@) with fallback to raw contact data. **Anti-duplicati (4-layer, both manual & Hunter)**: Before inserting search results, checks existing `lead_scraper_results` for same consultant by: (1) Google Maps `place_id` (most reliable), (2) business name (case-insensitive), (3) phone (normalized), (4) website domain — skips duplicates in-memory via Set lookup. Google Search also appends `-site:domain` exclusions (max 30) to avoid re-crawling known domains. **Pagination compensation**: Both manual search (POST /search) and Hunter autonomous search paginate SerpAPI (max 3 extra pages) to compensate for filtered duplicates, advancing by fixed page sizes (10 for Web, 20 for Maps). **Re-scrape endpoint**: `POST /api/lead-scraper/rescrape` resets `scrape_status`/`website_data`/`sales_summary` for a given search (or all), then re-runs Firecrawl enrichment + AI analysis in background. Frontend "Ri-analizza siti" button in search toolbar. **SerpAPI Maps enhancements**: Uses GPS coordinates (`ll` param) for 60+ Italian cities; saves `google_place_id`, `business_types`, `price_range`, `open_state`, `maps_description`. **Auto-retry**: When search returns 0 results, Gemini AI reformulates query using Italian commercial terms (max 1 retry). **Firecrawl enhancements**: LLM JSON schema extraction (emails/phones/services/team_members), Batch scrape (`/v1/batch/scrape`), contact page auto-detection via `links` format. **Safety guard**: `checkOutreachSafety` (in `outreach-safety-guard.ts`) now also checks `proactive_leads` table — blocks outreach if lead phone already exists with non-new status (contacted/client/not_interested). `max_leads_per_batch` is configurable from frontend (slider, default 15). Four modes: **Diretto** (every 30 min finds 1 lead, AI decides channel/message, executes immediately — voice→`scheduled_voice_calls`, WA→`ai_scheduled_tasks`, email→nodemailer — creates `proactive_leads` entry with `source='hunter'`; logged in `hunter_actions` table), Full Autonomo (tasks auto-scheduled), Piano Interattivo (AI plan → chat → approve), Solo Approvazione (tasks created as `waiting_approval`). Features: unified outreach queue with channel/status filters, collapsible activity timeline, configurable multi-step follow-up sequences, post-call transcript AI classification with automatic next actions, KPI conversion metrics. Config includes `email_account_id`, `whatsapp_config_id`, `voice_template_id` for channel routing. Direct mode executor: `server/ai/hunter-direct-executor.ts`. API: `GET /api/ai-autonomy/hunter/actions`, `POST /api/ai-autonomy/hunter/trigger-direct`. Hunter-found leads appear in `/consultant/proactive-leads` with teal "Hunter" badge.
    *   **Auto Proactive Lead Registration (`ensureProactiveLead`):** Utility in `server/utils/ensure-proactive-lead.ts` — any contact reached via any channel (voice call, WhatsApp outbound, scheduled task) is automatically registered in `proactive_leads` if not already present (phone dedup via unique constraint). Integrated at: POST /api/ai-autonomy/tasks (manual task creation), executeSingleWhatsApp (after WA send), handleSuccess (voice call initiated), POST /voice/outbound/trigger (direct call), POST /voice/outbound/schedule (scheduled call). Uses ON CONFLICT upsert to handle race conditions. Status set to 'pending' for scheduled items, 'contacted' for executed ones. Now also triggers `scheduleAutoCall()` when a new lead is created (`created === true`).
    *   **Auto-Call System:** `server/services/auto-call-scheduler.ts` + `server/services/call-timer-manager.ts` — Every lead entering proactive-leads automatically triggers a voice call + WhatsApp simultaneously. **Entry points**: POST /proactive-leads (manual), CSV/Excel import (`lead-import-router.ts`), Google Sheets polling (`sheets-polling-scheduler.ts`), webhook/internal (`ensure-proactive-lead.ts`). **Excluded**: Hunter (`hunter-direct-executor.ts`) manages its own calls. **Anti-duplicate**: checks `scheduled_voice_calls` for same phone+consultant with active status before scheduling. **SIP validation**: skips if consultant has no SIP configured. **Batch support**: `scheduleAutoCallBatch()` staggers calls by 5 min per lead for CSV/Sheets imports. **Call Timer Manager**: Singleton `call-timer-manager.ts` extracts timer Map + `scheduleTimer`/`cancelTimer`/`registerCallFunctions` from voice-router closure; initialized at voice-router load, accessible from auto-call-scheduler via `getCallTimerManager()`. **Concurrency check**: `executeOutboundCall` queries active `calling`/`talking` calls and `max_concurrent_calls` from `consultant_availability_settings`; if line full, requeues +3 min without incrementing attempt counter. **Queue drain**: when a call completes/fails, `drainCallQueue()` picks next pending call for same consultant. **UI**: proactive-leads table shows call status badge (color-coded: Schedulata/Programmata/In corso/Completata/Fallita/Retry) with tooltip; detail dialog shows "Chiamata Voice" section; create form has optional "Istruzioni chiamata" textarea. **Endpoint**: `GET /api/proactive-leads/call-status?phones=...` returns latest call status per phone.
    *   **Assisted Mode (Modalità Assistita):** Allows AI tasks to pause for consultant feedback and context injection.
    *   **Intelligent Document Generation:** Generates dual output (summary and formal document) with dynamic classification and adaptive PDF layouts.
    *   **Voice Supervisors (LLM Architecture):** Real-time transcript analysis for task management and booking.
    *   **Sales Hub (Unified Sales Center):** A single hub page (`sales-hub.tsx`) at `/client/sales-agents` (client) and `/consultant/sales-agents` (consultant) combines 3 tabs: **Agenti AI** (agent list from `client-sales-agents-list.tsx`), **Script Manager** (`client-script-manager.tsx`), and **Live Consultation** (`live-consultation.tsx`). Each sub-page supports an `embedded` prop that strips sidebar/header wrappers when rendered inside the hub. Tab design uses modern gradient icons with animated indicator. Client sidebar entry: "Sales Hub" under Dipendenti AI. Consultant sidebar entry: "Venditori Autonomi" under COMUNICAZIONE. Backend endpoints in `sales-agent-config.ts` and `sales-scripts.ts` accept both `client` and `consultant` roles. Frontend `AuthGuard` supports array of roles (`requiredRole={["client", "consultant"]}`). Sub-routes for config (`:agentId`), analytics (`:agentId/analytics`), and scripts (`:agentId/scripts`) remain as separate full pages. Live-consultation page also accessible standalone at `/live-consultation`.
*   **AI Autonomy UI Structure:** The Dipendenti AI page (`/consultant/ai-autonomy`) is the **central operational hub** with a unified scrollable tab bar. Core tabs: Dipendenti AI, Autonomia, Feed, Task. Embedded tool tabs (separated by a visual divider): Documenti KB, WhatsApp, Lead Scraper, Chiamate. Each embedded tab lazy-loads the corresponding page component (`consultant-knowledge-documents.tsx`, `consultant-whatsapp.tsx`, `consultant-lead-scraper.tsx`, `consultant-voice-calls.tsx`) with `embedded={true}`, which strips Sidebar/Navbar/layout wrappers so they render cleanly inline. All 4 pages remain accessible at their standalone routes too. The embedded tab triggers use colored active states (amber/green/violet/emerald) to visually distinguish them from core tabs. It features Kanban UX with scroll-snap columns, drag-to-scroll, prominent status badges, and conversational activity logs where AI employees "talk" to the consultant.
*   **Bidirectional Telegram Bot Integration:** Each AI employee can have its own Telegram bot, supporting 1:1 private and group chats with bidirectional message syncing and an "Open Mode" for AI-driven conversational onboarding. **Streaming responses**: Uses `generateContentStream` + `editMessageText` for progressive message delivery (sends "⏳" placeholder, then updates every 500ms with accumulated text + "▌" cursor). **Mid-generation cancellation**: If user sends a new message while AI is generating, the current generation is aborted via `AbortController`, the partial streaming message is deleted, all messages are combined, and a new generation starts — ensures never 2 AI messages in a row. Buffer delay reduced to 500ms (private) / 1000ms (onboarding) for near-instant responsiveness. **Open Mode Memory Continuity**: On onboarding completion, the full conversation is injected into `telegram_open_mode_messages` (with idempotency check to prevent duplicates), so post-onboarding AI sees the entire history. The open mode system prompt loads the complete user profile (name, job, goals, desires, full_profile_json, onboarding_summary) from `telegram_user_profiles`. All messages loaded up to 32K char budget (no arbitrary LIMIT).
*   **Agent Chat Memory Protection:** Messages in `agent_chat_messages` are NEVER deleted — no auto-cleanup, no manual clear endpoint, no UI button. The previous auto-cleanup (deleted all but last 30 when >60) was removed. The DELETE endpoint returns 403. Daily summaries are generated via `agent-daily-summary-scheduler.ts` and served by `GET /agent-chat/:roleId/daily-summaries` (returns summaries + `missing_days` + `missing_count`). Memory UI in SettingsTab.tsx shows missing days banner with "Genera tutti" button, formatted HTML summaries via `formatSummaryHtml`, and green checkmark when all days have summaries.
*   **Telegram Send Mode:** `telegram_send_mode` column on `ai_autonomy_settings` (values: 'streaming' or 'single', default 'streaming'). In **streaming** mode, the AI response is progressively revealed on Telegram via `editMessageText` updates (20 chars every 600ms). In **single** mode, the full response is generated first, then sent as one complete message. Toggle in Configurazione Avanzata (Step 1) under "Modalità invio Telegram". The `flushPrivateBuffer()` in `telegram-service.ts` reads the setting from DB before generating.
*   **QR Code Share System (Custom Slug + Permanent Links):** Share links support custom slugs (`is_custom_slug` boolean on `whatsapp_agent_shares`). Custom slug shares are protected from accidental deletion (DELETE returns 403, requires POST `/:shareId/unlock` first). Slug validation: `a-z`, `0-9`, `-`, 3-50 chars. UI in `agent-share-manager.tsx` has "Link Permanente per QR Code" toggle with live preview. **Conversation Message Count Fix:** DB trigger `trg_increment_message_count` auto-increments `message_count` + `last_message_at` on `whatsapp_agent_consultant_conversations` after each message insert. Cleanup function (`cleanupEmptyConsultantAgentConversations`) now checks real message count via `NOT EXISTS` subquery instead of cached `message_count` field.
*   **Public Link Chat Dashboard:** Page `/consultant/whatsapp-agents-chat` shows a 3-panel layout (agents sidebar → conversations list → chat view) for monitoring all public link conversations. No truncations, no limits. Mobile-responsive with back navigation. Endpoint: `GET /api/whatsapp/agent-share/:shareId/conversations` returns all conversations with real message count.
*   **Robert Sales Coach (AI Autonomous Employee):** Robert is the Sales Coach persona in both the Delivery Agent panel (singleton `sales_coach` session with persistent chat) and the AI Autonomy panel (autonomous employee). The AI Autonomy chat for Robert reads/writes directly to the `delivery_agent_messages` table (the sales_coach session), so messages sent from either panel appear in both. Robert's system prompt includes the full MANUALE-COMPLETO-LIGHT.md inline plus support for custom KB documents attached via `linkedKbDocumentIds` in `agent_contexts.robert`. Defined in `ai-autonomous-roles.ts` with categories `["sales", "preparation", "monitoring"]`, accent color amber.
*   **Global Consultation Store (Marco + Alessia):** Aggregates client consultation notes and email journey documents into a single consultant-specific store, optimized for AI access. Alessia also loads the Global Store, recent WhatsApp conversations, and AI-assistant chat history.
*   **Voice Latency Optimization System:** Implements in-memory caching for voice-critical data, scheduled call query deduplication, single `voiceThinkingBudget` application, first-message barge-in protection, deferred prompt injection, and configurable VAD (Voice Activity Detection). Includes smart DB warmup and WS warmup mode. Alessia Enhanced Intelligence System features persistent client memory, client-specific measurable objectives, automatic post-consultation detection, dynamic call scripts, and an AI-powered feedback loop.
*   **Operational Data File Search Optimization:** Reduces AI chat system prompt token count by syncing operational data as `sourceType: "operational_context"`.
*   **File Search Security Hardening:** `resolveClientAssistantStores` validates ownership of `target_client_ids`, `target_department_ids`, and `target_employee_ids` against `consultant_id` before syncing. PUT endpoint uses `cleanupStaleStoresForDocument()` to atomically remove stale stores and add missing ones. "Sincronizza ora" button also triggers `POST /resync-stores` for store-level cleanup. Private Store display query filters to active consultant clients only. Library category unassignment (`removeLibraryCategoryFromClient`) and university year unassignment (`removeUniversityYearFromClient`) clean up File Search documents from client stores with ownership validation. The "Private Store clienti" badge shows a clickable popover listing each client name with their sync status.
*   **Consultant Detailed Profile:** Stores comprehensive consultant information across 8 sections, integrated into the AI's understanding.
*   **System Robustness:** Includes a 4-week calendar, universal PDF support, real-time Google Drive sync, a Dataset Sync API, a Data Sync Observability Dashboard, an Intent Follow-Through System, a Partner Webhook Notification System, and a Database-Based Cron Mutex. An Anti-Zombie Connection System prevents stale Gemini WebSocket connections. **Ghost Call Detection**: VPS `/health` queries FreeSWITCH via ESL `show calls as json` and returns `freeswitchCalls` array + count alongside bridge session data. Frontend "Centro Chiamate AI" shows a sync status card comparing FreeSWITCH calls vs bridge sessions — green "Centralino sincronizzato" when matched, red "Chiamate fantasma rilevate" with per-call details and Kill buttons when mismatched. VPS `POST /freeswitch-kill` endpoint (service token auth) sends `uuid_kill` via ESL; Replit `POST /api/voice/freeswitch-kill` proxies it.
*   **AI Streaming Architecture:** The system handles streaming responses from Google Gemini API and Vertex AI, mapping SDK chunks to `GeminiStreamChunk` objects and yielding typed SSE events for `code_execution`, `code_execution_result`, `generated_file`, `function_call`, and `thinking`.

*   **Per-Consultant Overflow Queue Audio Management:** Custom hold music and announcements per-consultant for the FreeSWITCH overflow queue. Audio files stored at `uploads/overflow-audio/{consultantId}/`. 10 audio slots: hold_A-E (rotating hold messages), position_prefix/suffix (queue position announcements), transferring, transfer_failed, timeout. Backend endpoints in `voice-router.ts`: generate via Gemini TTS (`POST /overflow-audio/generate`), manual upload (`POST /overflow-audio/upload`), list/delete/play/defaults. All audio auto-converted to 8kHz 16-bit mono WAV for FreeSWITCH via `convertWavTo8kMono()`. VPS download endpoints use service token auth (same as number-lookup). VPS `session-manager.ts` syncs audio to `/opt/sounds/overflow/{consultantId}/` with 24h cache and stale file cleanup. Dialplan `05_overflow_queue.xml` uses `${overflow_audio_dir}` channel variable (default: `/opt/sounds/overflow/default`). UI: "Configurazione" tab reorganized into 4 sub-tabs (Numeri & Voce, Coda d'Attesa, Chiamate Uscita, Connessione). "Audio Coda d'Attesa" card in Coda d'Attesa sub-tab with per-slot TTS generation, upload, preview, and delete.
*   **Scheda Contatto (Contact Profile):** Unified contact profile page at `/consultant/voice-calls/contact/:phone` aggregating call history, scheduled calls, Hunter data, proactive lead info, and retry status. API endpoint: `GET /api/voice/contact/:phone`. Also accessible via `/consultant/voice-calls/:id` (resolves callId to phone via `/api/voice/contact-by-call/:callId`). Features tabbed call history with accordion expansion, sidebar cards for profile/Hunter/lead/retry data.
*   **VoIP Number Provisioning (Inventory-Based Flow):** Two-level VoIP number management. **Admin** pre-purchases numbers from Telnyx API and stores them in `number_inventory` table. **Consultant** picks from available inventory (not Telnyx directly) and gets assigned instantly, then has 7 days for KYC documentation. **Architecture: Centralized IP Trunk** — all Telnyx numbers use a single global IP connection (`telnyx-ip` → `orbitale.sip.telnyx.com`). Config in `system_settings` (keys: `telnyx_api_key`, `telnyx_ip_connection_id`, `telnyx_webhook_public_key`). Service: `server/services/telnyx-provisioning.ts`. **DB tables**: `number_inventory` (admin-managed pool: available/assigned/reserved/released), `consultant_numbers` (number ownership, KYC status, deadline tracking), `voip_provisioning_requests` (KYC submission data), `voip_provisioning_documents` (uploaded docs). **Admin endpoints** (`server/routes/admin.ts`): `GET/POST/PATCH/DELETE /api/admin/number-inventory/*` — search Telnyx, purchase, list, release, update notes. **Consultant endpoints** (`server/routes/voice-router.ts`): `GET available-numbers` (reads from inventory), `POST select-number` (assigns from inventory, no Telnyx call), `GET my-number`, `POST upload-kyc-doc`, `POST submit-kyc-docs`, `DELETE kyc-doc/:id`. **Admin UI**: "Inventario Numeri VoIP" card in admin-settings VPS/Voice tab — search, bulk purchase, inventory table with status badges. **KYC mapping**: Requirements use `requirement_id` cross-referenced with global requirement_types. **KYC deadline enforcement**: Cron poller suspends numbers with expired KYC deadlines. **Form validation**: Backend validates fiscal code (16 alphanum), P.IVA (11 digits), email format, CAP (5 digits). Frontend shows inline errors.
*   **Public Lead Magnet (`/onboarding-gratuito/:consultantId`):** Unauthenticated landing page where prospects submit name/email/phone, chat with Luca (AI onboarding consultant) via SSE streaming, and receive a personalized AI report. **Auto-Account Creation & Auto-Login**: On form submit, backend creates a `client` user with `is_lead_magnet=true`, generates JWT for immediate auto-login, and redirects to `/lead/chat`. Welcome email with credentials sent for future logins. **Lead Magnet Dashboard** at `/lead/chat` (`client/src/pages/lead-chat.tsx`): authenticated page with DeliveryChat + DeliveryReport components using `publicToken` prop (switches to public API endpoints, no consultant auth). PhaseIndicator, Chat/Report tabs, logout button. **Access Restriction**: `lead_magnet` tier users are globally blocked from all routes except `/lead/chat` via AuthGuard, RoleBasedRedirect, and login redirect. **getUserTier()** in `server/routes.ts` checks `is_lead_magnet` column as first priority (before gold/silver/bronze). JWT includes `type: 'lead_magnet'` + `consultantId` without requiring subscriptionId. **Dynamic per-consultant URL**: each consultant gets their own link (`/onboarding-gratuito/{userId}`). Backend: `server/routes/public/lead-magnet.ts` (4 public endpoints + account creation + email), `server/routes/lead/session.ts` (`GET /api/lead/my-session` authenticated). Frontend: `client/src/pages/public-lead-magnet.tsx` (auto-login + redirect), `client/src/pages/lead-chat.tsx` (dashboard). DB columns: `users.is_lead_magnet`, `delivery_agent_sessions.lead_user_id` + existing public columns. Docs: `docs/MARKETING/LEAD-MAGNET-IMPLEMENTAZIONE.md`.
*   **Delivery AI Agent (Dipendente Delivery):** AI-powered onboarding and delivery assistant at `/consultant/academy` (Delivery AI tab). Full-screen layout (bypasses max-w-7xl container when active). Four modes: **Onboarding** (consultant analyzes own business), **Discovery** (analyzes third-party client), **Simulator** (AI vs AI auto-play "Campo di Battaglia" — pick from 20 niches and 6 attitudes, an AI client plays the role while Luca conducts discovery automatically; SSE streaming via `POST /simulator/run`, stoppable via `POST /simulator/stop`; `getSimulatedClientPrompt()` generates the client persona), and **Sales Coach** (Marco persona — teaches consultants how to sell the 10 service packages; select a single package or "Panoramica Completa" for all; uses `getSalesCoachPrompt()` in `delivery-agent-prompt.ts` with Marco's direct/provocative Executive Coach style; sessions start in `assistant` status — no discovery/report phases; covers positioning, objection handling, upsell strategies, killer phrases; amber/gold UI theme). Four phases: Discovery (9-phase deep conversation: Chi Sei → Clienti → Come Lavori → Comunicazione & Assistenza Clienti → Vendita → Formazione → Team → Obiettivi → **Dettagli Attività** (nome_attivita, sito_web, citta_operativa), targeting 20-30 messages; FASE 4 includes customer support questions about post-sale assistance workload, recurring client requests, and interest in trainable AI assistant; `assistenza_clienti` JSON block in profile) → Elaborazione (profile extraction with `[DISCOVERY_COMPLETE]` tag, rich JSON with fields for all 9 phases) → Report (rules 33-34: always recommend trainable AI assistant for SETTER AI/DIPENDENTI AI packages; highlight as key time-saver when discovery reveals heavy support workload) → Assistente (permanent delivery companion mode with package-aware guidance). **Discovery Complete Dialog**: Animated `DiscoveryCompleteDialog` component (framer-motion) shown on `phase_change` to `elaborating` — success icon with spring animation, gradient title, staggered phase list (5 items with 150ms delay), gradient CTA button; replaces old amber banner with smaller reopen button. Uses `hasShownDialogRef` to prevent duplicate auto-opens. Uses Gemini with thinking enabled via `getAIProvider()`. **Google Business Pre-Enrichment**: Before report generation, scrapes Google Maps via SerpAPI (rating, reviews, address) and website via Firecrawl (services, social links, team members) using keys from `superadminLeadScraperConfig` DB or env vars. Scraping has 15s/20s timeouts and fails silently. **Report generation is multi-step agent flow**: STEP 1 (Analysis + business intelligence) → STEP 2 (Draft, 32K tokens) → STEP 3 (Review, 32K tokens). **Catalogo Completo (Parte 2)**: All 10 service packages with personalized descriptions, 2-3 concrete examples per package, full module lists, and `gia_consigliato` flag. Clean typographic design (no colored badges), italic examples with left border, gray pill chips for modules. **Manuale Injection**: `MANUALE-COMPLETO-LIGHT.md` (136K chars compressed from 237K) loaded at startup and injected into both ASSISTANT_MODE_PROMPT and `getReportGenerationPrompt()`. **Auto File Search Bootstrap**: On first chat message, checks `file_search_documents` for `consultant_guide` indexed; if missing, triggers `FileSearchSyncService.syncConsultantGuide()` (retries on failure via in-memory Set). **Rich report structure**: `lettera_personale`, per-package deep analysis with `punteggio` (1-10), `cosa_va_bene`, `cosa_non_funziona`, `come_correggere`, `diagnosi_critica`, `tabella_diagnostica`, `insight_chiave`, 4-week roadmap with KPIs, `azioni_questa_settimana` (3 priority actions), `chiusura_personale`. **Frontend**: Two-column layout with sticky sidebar chapter navigation (IntersectionObserver) and scrollable long-form document. Print CSS with page breaks. **PDF generation**: Server-side pdfkit endpoint at `GET /reports/:sessionId/pdf`. **Animated generation progress**: 5-phase progress indicator with timed transitions. DB tables: `delivery_agent_sessions`, `delivery_agent_messages`, `delivery_agent_reports`. Backend: `server/routes/delivery-agent.ts`. Prompt: `server/prompts/delivery-agent-prompt.ts`. Frontend: `client/src/components/delivery-agent/`.
*   **Funnel Flow Builder (Content Studio Tab 4):** Visual drag-and-drop funnel builder at `/consultant/content-studio/ideas` (4th tab: "Funnel Builder" with cyan/teal gradient and GitBranch icon). Built with `@xyflow/react` (React Flow v12). **Components**: `client/src/components/funnel-builder/` — `FunnelBuilderTab.tsx` (main container with 3-column layout: palette | canvas | config panel), `FunnelNode.tsx` (custom node with category-colored border, icon, linked entity preview), `FunnelEdge.tsx` (animated bezier edge with flowing dots), `FunnelPalette.tsx` (left sidebar with collapsible categories and draggable items), `NodeConfigPanel.tsx` (right panel for title/subtitle/notes/conversion rate editing + entity picker). **Node Types** (`funnel-node-types.ts`): 27 types across 7 categories — Sorgenti (blue: facebook_ads, google_ads, instagram_ads, tiktok_ads, offline_referral, organic), Cattura (green: landing_page, form_modulo, lead_magnet, webhook), Gestione (violet: import_excel, crm_hunter, setter_ai), Comunicazione (indigo: whatsapp, email, voice_call, sms, instagram_dm), Conversione (orange: appuntamento, prima_call, seconda_call, chiusura, pagamento), Delivery (emerald: onboarding, servizio, followup), Custom (gray: custom_step). **Entity Linking (Redesigned)**: Each node type maps to a specific platform entity via `getEntityTypeForNode()` in `funnel-node-types.ts`. 12 entity types: `posts` (content_posts, platform-filterable), `referral_config` (referral_landing_config), `optin_config` (optin_landing_config), `lead_magnet` (consultant_availability_settings + referral check), `hunter_searches` (lead_scraper_searches), `ai_employees` (AI_ROLE_DEFINITIONS + enabled_roles from ai_autonomy_settings), `agents` (consultant_whatsapp_config), `email_accounts` (email_accounts), `voice_numbers` (voice_numbers), `booking` (consultant_availability_settings booking config), `services` (service_catalog_items), `campaigns` (marketing_campaigns). **Leonardo — Architetto dei Funnel (AI Employee)**: The funnel chat is now a proper AI employee (dipendente AI, role ID `architetto`, name "Leonardo", accent cyan) using the same `processAgentChatInternal` / `agent_chat_messages` / 32K-char context system as all other employees (Marco, Alessia, Hunter, etc.). Registered in `AI_ROLES` in `server/cron/ai-autonomous-roles.ts` with `maxTasksPerRun: 0` (chat-only, no autonomous tasks). `fetchRoleData` loads: (1) consultant's existing funnels from `consultant_funnels`, (2) brand voice from `content_studio_config.brandVoiceData`, (3) a selected Content Studio template from `content_idea_templates` (niche, target, objective, full 7-phase market research data). Template linking stored in `ai_autonomy_settings.agent_contexts->'architetto'->'linkedTemplateId'` JSONB, managed via `GET/PUT /api/ai-autonomy/agent-context/architetto/template`. Chat panel (`FunnelChat.tsx`) calls `POST /api/ai-autonomy/agent-chat/architetto/send` (standard JSON fetch, no SSE) and loads history from `GET /api/ai-autonomy/agent-chat/architetto/messages`. Funnel JSON `[FUNNEL_START]...[FUNNEL_END]` tags are parsed client-side with full node type/category mapping. Template selector dropdown at top of chat panel fetches templates via `GET /api/content/idea-templates` and persists selection. `buildPrompt` enriches the architect prompt with `BRAND VOICE & IDENTITÀ`, `RICERCA DI MERCATO` (7-phase), `TARGET & NICCHIA`, and `FUNNEL ESISTENTI` sections. Old SSE endpoints (`POST /api/funnels/chat`, `GET /api/funnels/chats`) removed; `consultant_funnel_chats` table kept for existing data. 9 node types have no entity (landing_page, webhook, import_excel, sms, instagram_dm, prima_call, seconda_call, chiusura, custom_step) and show "Configurazione manuale" in panel. `getEditLinkForEntity()` returns the correct consultant page route for each entity type — "Modifica Fonte" button navigates there via wouter. `getPlatformFilterForNode()` provides default platform filter for ad node types. NodeConfigPanel shows type-specific rich entity cards (LinkedEntityCard component) with contextual fields per entity type: post thumbnails+platform badges, AI employee accent-color avatars, booking slug URLs, service price badges, campaign conversion rates, etc. FunnelNode shows compact entity previews (EntityPreview component) per type. **AI Generation**: `POST /api/funnels/generate` with enhanced Gemini prompt including entity mapping context, subtitle suggestions per node type, category validation, and better parallel positioning logic. **Backend**: `server/routes/funnel-router.ts` — CRUD, AI generation, 12 entity lookup endpoints (posts with platform filter, referral-config, optin-config, lead-magnet, hunter-searches, ai-employees, email-accounts, booking, campaigns, agents, voice-numbers, services). All endpoints return normalized `name` field. **DB**: `consultant_funnels` table (id, consultant_id FK, name, description, nodes_data JSONB, edges_data JSONB, is_active, created_at, updated_at). Schema in `shared/schema.ts`.
*   **Service Store (Internal E-Commerce):** Consultant-managed service catalog at `/consultant/catalog-settings` with 3 product creation modes: full AI from prompt, manual + AI description, 100% manual. Products have categories (AI, Marketing, Automation, Communication, Analytics, Other), support singles + bundles, and two payment modes: Stripe Connect (50% rev share) or Direct/BYOK (100%). Clients browse at `/client/store`, consultants-as-clients at `/consultant/store` (shows upline's catalog via `users.consultantId`). Purchase flow via Stripe Checkout (Connect) or direct link redirect. Dashboard integration shows active purchases. DB tables: `service_catalog_items`, `client_purchased_items`. Backend: `server/routes/service-catalog-router.ts`. AI generation uses `GEMINI_3_MODEL` via `getAIProvider()`.
*   **Lead Scraper Email Verification & Smart Contact Selection:** MX+SMTP email verification service (`server/services/email-verification-service.ts`) — zero external API cost. DNS MX lookup, SMTP RCPT TO handshake, catch-all detection, disposable domain list (~500 domains), syntax validation. Returns `{ is_valid, status, confidence }`. Smart contact selector (`server/services/contact-selector.ts`) prioritizes: email (verified personal > verified business > catch_all > unverified), phone (mobile 3xx > Maps listing > website scraped > landline 0xx). All contacts stored in `all_contacts` JSONB with `primary_email`/`primary_phone` auto-selected. Frontend shows verification badges (green/red/orange/yellow), priority ranking, manual override, and on-demand re-verification button.
*   **Skill Store:** A marketplace for importing and creating AI skills, which are markdown instructions injected into the Gemini AI system prompt. Supports file uploads and code execution.
*   **Lead Scraper:** A dual-engine search via SerpAPI (Google Maps and Google Search) with website enrichment via Firecrawl. Features smart caching, CSV export, CRM lead management with status tracking, notes, and deal values. Includes an AI Sales Agent for generating structured sales reports and AI Keyword Suggestions for search queries. A slide-in chat panel provides conversational interaction. **Search Modes**: 3 modes via segmented control — "Solo Cerca" (SerpApi only, no AI), "Predefinito" (search + Firecrawl + AI analysis), "Cerca + Outreach" (search + AI + auto-schedule outreach tasks). Mode stored in search metadata (`metadata.params.searchMode`). **CRM Outreach Filters**: Sidebar includes "Outreach" section filtering by task status (In Approvazione, Schedulati, In Corso, Contattati, In Trattativa, Non Raggiungibile) via LEFT JOIN with `ai_scheduled_tasks` on `outreachTaskId`. **Cascade Delete**: Deleting leads also removes associated outreach tasks from `ai_scheduled_tasks`. **Auto-Status Sync**: Lead `contactedChannels` and `leadStatus` are updated after actual delivery (not just task creation) for voice, WhatsApp, and email channels. Failed outreach sequences set `leadStatus` to `non_raggiungibile`. **Smart Location System**: Local Italian cities/regions database (~100 cities, 20 regions) with fuzzy matching (Levenshtein distance), aliases (province codes, English names), and population-weighted scoring. `searchItalianLocations()` provides instant autocomplete; `normalizeLocation()` auto-corrects typos and abbreviations (e.g., "mi"→"Milano", "milno"→"Milano", "toscna"→"Toscana"). Backend normalizes location before SerpAPI queries. Frontend shows structured suggestions (city + region) and "corretto" badge in confirm dialog when auto-corrected. Service: `server/services/lead-scraper-service.ts`. Endpoints: `GET /autocomplete/location`, `GET /normalize-location`.

# External Dependencies
*   **Supabase**: PostgreSQL hosting.
*   **Recharts**: Data visualization.
*   **Date-fns**: Date manipulation.
*   **Radix UI**: Accessible UI primitives.
*   **Google Fonts**: Inter, Poppins, DM Sans, Fira Code.
*   **Lucide React**: Iconography.
*   **Google Gemini API**: Powers AI features.
*   **Percorso Capitale API**: Financial management system.
*   **Fathom**: Consultation recording and transcription.
*   **Driver.js**: Interactive guided tours.
*   **Twilio API**: WhatsApp Business messaging.
*   **X (Twitter) API v2**: Direct message automation and Account Activity webhooks.
*   **Stripe**: Payment processing for subscriptions and licenses.
*   **FreeSWITCH**: Voice telephony integration.
*   **Google Calendar API**: Appointment scheduling.
*   **Publer**: Social media scheduling and publishing.
*   **SerpAPI**: Google Maps business data extraction for Lead Scraper.
*   **Firecrawl**: Website scraping for email/contact extraction in Lead Scraper.
*   **Telnyx API v2**: VoIP number provisioning, SIP connections, managed accounts for multi-tenant telephony.
*   **@xyflow/react**: React Flow v12 for the Funnel Flow Builder (drag-and-drop canvas).
*   **dagre**: Auto-layout engine for AI-generated funnels.

# Academy Modules
The Academy system (`/consultant/academy`) has 16 modules total:
- 6 original setup/workflow modules (setup_base, acquisisci_lead, vendi_converti, automazioni_ai, contenuti_corsi, avanzato)
- 10 service package modules (pkg_setter_ai, pkg_dipendenti_ai, pkg_hunter, pkg_email_journey, pkg_lavoro_quotidiano, pkg_formazione, pkg_content_studio, pkg_voce_ai, pkg_pagamenti, pkg_team) with 30 lessons containing deep tutorial content
- Content stored in `academy_lessons.content` column (TEXT), rendered with markdown-like formatting in LessonDetail component
- Master content source: `GUIDA-PACCHETTI-SERVIZIO.md`
- Seed in `server/routes/academy.ts` covers modules + lessons (content populated via direct SQL from guide)
- API uses `SELECT *` so content field is auto-included
- Academy page has 3 tabs: Accademia (modules/lessons), Delivery AI (delivery agent chat), Assistente Setup (onboarding chat with activation status)
- `getOnboardingStatusForAI(consultantId)` exported from `server/routes/onboarding.ts` — returns activation step statuses for AI prompts
- Delivery Agent system prompt includes real-time "STATO CENTRO DI ATTIVAZIONE" block via `getDeliveryAgentSystemPrompt()` in `server/prompts/delivery-agent-prompt.ts`