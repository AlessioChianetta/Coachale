<!--
  ================================================================
  Dialplan: ACL Security Check (Public Context) - DISABLED
  ================================================================
  
  mod_acl is NOT available in this FreeSWITCH Docker build.
  UFW firewall provides network-level IP filtering instead.
  Only ports 5080/udp and 5080/tcp are open for SIP.
  
  This file is intentionally empty (no ACL check).
  ================================================================
-->

<include>
  <extension name="acl_security_log" continue="true">
    <condition>
      <action application="log" data="INFO [INBOUND] Chiamata da ${network_addr} → ${destination_number} (caller: ${caller_id_number})"/>
    </condition>
  </extension>
</include>
