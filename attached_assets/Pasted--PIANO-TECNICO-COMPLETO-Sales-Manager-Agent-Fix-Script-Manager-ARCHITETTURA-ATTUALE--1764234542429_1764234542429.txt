📋 PIANO TECNICO COMPLETO
Sales Manager Agent + Fix Script Manager
🏗️ ARCHITETTURA ATTUALE
┌─────────────────────────────────────────────────────────────────────────────┐
│                           FLUSSO ATTUALE                                     │
└─────────────────────────────────────────────────────────────────────────────┘
┌──────────────┐     Audio      ┌──────────────────┐     WebSocket     ┌─────────────┐
│   Prospect   │ ◄────────────► │  Gemini Live API │ ◄───────────────► │  Browser    │
│   (Telefono) │                │   (AI Sales)     │                   │  (Client)   │
└──────────────┘                └────────┬─────────┘                   └─────────────┘
                                         │
                                         │ Ogni turnComplete
                                         ▼
                               ┌──────────────────┐
                               │ StepAdvancement  │  ← LIMITATO!
                               │     Agent        │     Solo step advancement
                               └──────────────────┘     + feedback base

Problemi attuali:
StepAdvancementAgent fa solo una cosa: decide se avanzare allo step
Non rileva segnali di acquisto, obiezioni, problemi di tono
Mutex bloccante: se sta analizzando, salta i turni successivi
Confusione messaggi: a volte traccia messaggi AI come se fossero utente
🎯 NUOVA ARCHITETTURA: SALES MANAGER AGENT
┌─────────────────────────────────────────────────────────────────────────────┐
│                           NUOVA ARCHITETTURA                                 │
└─────────────────────────────────────────────────────────────────────────────┘
┌──────────────┐     Audio      ┌──────────────────┐     WebSocket     ┌─────────────┐
│   Prospect   │ ◄────────────► │  Gemini Live API │ ◄───────────────► │  Browser    │
│   (Telefono) │                │   (AI Sales)     │                   │  (Client)   │
└──────────────┘                └────────┬─────────┘                   └─────────────┘
                                         │
                    ┌────────────────────┼────────────────────┐
                    │                    │                    │
                    ▼                    ▼                    ▼
         ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
         │ Message Buffer  │  │ Sales Manager   │  │ Feedback Queue  │
         │ (User vs AI)    │  │     Agent       │  │ (Priority-based)│
         └─────────────────┘  └────────┬────────┘  └─────────────────┘
                                       │
              ┌────────────────────────┼────────────────────────┐
              │                        │                        │
              ▼                        ▼                        ▼
    ┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
    │ Signal Detector │     │ Checkpoint      │     │ Tone Monitor    │
    │ - Buy signals   │     │ Validator       │     │ - Anti-robot    │
    │ - Objections    │     │ - Phase gates   │     │ - Energy check  │
    └─────────────────┘     └─────────────────┘     └─────────────────┘

📦 COMPONENTE 1: FIX SCRIPT MANAGER (Opzione A)
Problema Attuale
┌─────────────────────────────────────────────────────────────────────────────┐
│                        PROBLEMA: ID INSTABILI                                │
└─────────────────────────────────────────────────────────────────────────────┘
SALVATAGGIO:
┌────────────────┐    ┌────────────────┐    ┌────────────────┐
│ blockStructure │ ──►│ blocksToText() │ ──►│ content (text) │
│ id: "block_123"│    │ PERDE GLI ID!  │    │ "FASE #1..."   │
└────────────────┘    └────────────────┘    └────────────────┘
        │
        ▼
┌────────────────────────────┐
│ energySettings:            │
│ { "block_123": {level: "ALTO"} }  ← Salvato con ID vecchio
└────────────────────────────┘
RICARICAMENTO:
┌────────────────┐    ┌──────────────────┐    ┌────────────────┐
│ content (text) │ ──►│ parseTextToBlocks│ ──►│ blockStructure │
│ "FASE #1..."   │    │ GENERA NUOVI ID! │    │ id: "block_789"│ ← DIVERSO!
└────────────────┘    └──────────────────┘    └────────────────┘
        
energySettings["block_123"] → ❌ NON ESISTE PIÙ!

Soluzione: Usare structure come fonte di verità
┌─────────────────────────────────────────────────────────────────────────────┐
│                        SOLUZIONE: STRUCTURE = VERITÀ                         │
└─────────────────────────────────────────────────────────────────────────────┘
SALVATAGGIO:
┌────────────────┐    ┌────────────────┐    ┌────────────────┐
│ blockStructure │ ──►│ blocksToText() │ ──►│ content (text) │  ← Per display
│ id: "block_123"│    └────────────────┘    └────────────────┘
└───────┬────────┘
        │
        └──────────────────────────────────►┌────────────────┐
                                            │ structure JSON │  ← FONTE DI VERITÀ
                                            │ {id:"block_123"}│
                                            └────────────────┘
RICARICAMENTO:
┌────────────────┐
│ structure JSON │ ──► USA DIRETTAMENTE (ID preservati!) ✅
│ {id:"block_123"}│
└────────────────┘
energySettings["block_123"] → ✅ FUNZIONA!

File da modificare:
File	Modifica
client/src/pages/client-script-manager.tsx	Leggere da script.structure invece di parseTextToBlocks(script.content)
server/routes/sales-scripts.ts	Salvare structure JSON insieme a content
shared/script-parser.ts	Aggiungere funzione mergeStructureWithOverrides()
Codice chiave:
// PRIMA (problematico) - client-script-manager.tsx
useEffect(() => {
  if (script?.content) {
    const parsed = parseTextToBlocks(script.content); // ❌ Genera nuovi ID!
    setBlockStructure(parsed);
  }
}, [script]);
// DOPO (stabile)
useEffect(() => {
  if (script) {
    if (script.structure) {
      // ✅ Usa structure salvata (ID preservati!)
      setBlockStructure(script.structure as ScriptBlockStructure);
    } else if (script.content) {
      // Fallback per vecchi script senza structure
      const parsed = parseTextToBlocks(script.content);
      setBlockStructure(parsed);
    }
  }
}, [script]);

// Salvataggio - sales-scripts.ts
await db.update(salesScripts)
  .set({
    content: blocksToText(blockStructure),  // Per visualizzazione
    structure: blockStructure,               // ✅ Fonte di verità!
    energySettings: energySettings,
    ladderOverrides: ladderOverrides,
    updatedAt: new Date()
  })
  .where(eq(salesScripts.id, scriptId));

📦 COMPONENTE 2: SALES MANAGER AGENT
2.1 Struttura del nuovo agente
// server/ai/sales-manager-agent.ts
interface SalesManagerAnalysis {
  // Step advancement (come prima)
  stepAdvancement: {
    shouldAdvance: boolean;
    nextPhaseId?: string;
    nextStepId?: string;
    confidence: number;
    reasoning: string;
  };
  
  // 🆕 Segnali di acquisto rilevati
  buySignals: {
    detected: boolean;
    signals: Array<{
      type: 'price_inquiry' | 'timeline' | 'interest' | 'commitment';
      phrase: string;
      confidence: number;
    }>;
    suggestedAction?: string;
  };
  
  // 🆕 Obiezioni rilevate
  objections: {
    detected: boolean;
    objections: Array<{
      type: string;  // es. "no_time", "need_to_think", "too_expensive"
      phrase: string;
      suggestedResponse: string;  // Dallo script obiezioni
    }>;
  };
  
  // 🆕 Validazione checkpoint
  checkpointStatus: {
    currentCheckpoint?: string;
    isComplete: boolean;
    missingItems: string[];
    canAdvance: boolean;
  };
  
  // 🆕 Monitoraggio tono
  toneAnalysis: {
    isRobotic: boolean;
    consecutiveQuestions: number;
    lastMessageTooLong: boolean;
    energyMismatch: boolean;
    correction?: string;
  };
  
  // Feedback da iniettare (prioritizzato)
  feedbackForAgent?: {
    shouldInject: boolean;
    priority: 'critical' | 'high' | 'medium' | 'low';
    type: 'correction' | 'buy_signal' | 'objection' | 'checkpoint' | 'tone';
    message: string;
  };
}

2.2 Rilevamento Segnali di Acquisto
┌─────────────────────────────────────────────────────────────────────────────┐
│                     SEGNALI DI ACQUISTO - DISCOVERY                          │
└─────────────────────────────────────────────────────────────────────────────┘
TIPO              │ FRASI ESEMPIO                        │ AZIONE SUGGERITA
──────────────────┼──────────────────────────────────────┼─────────────────────
price_inquiry     │ "Quanto costa?"                      │ Non rispondere subito,
                  │ "Qual è l'investimento?"             │ prima qualifica il valore
                  │ "Che prezzo ha?"                     │
──────────────────┼──────────────────────────────────────┼─────────────────────
timeline          │ "Quando possiamo iniziare?"          │ Conferma interesse,
                  │ "Da quando è disponibile?"           │ procedi a closing
                  │ "In quanto tempo vedo risultati?"    │
──────────────────┼──────────────────────────────────────┼─────────────────────
interest          │ "Mi interessa"                       │ Approfondisci cosa
                  │ "Sembra interessante"                │ lo ha colpito
                  │ "Dimmi di più"                       │
──────────────────┼──────────────────────────────────────┼─────────────────────
commitment        │ "Ok, facciamolo"                     │ Procedi a closing
                  │ "Sono convinto"                      │ immediato
                  │ "Mi hai convinto"                    │

2.3 Rilevamento Obiezioni
┌─────────────────────────────────────────────────────────────────────────────┐
│                     OBIEZIONI COMUNI - DISCOVERY                             │
└─────────────────────────────────────────────────────────────────────────────┘
TIPO              │ FRASI RILEVATE                       │ RISPOSTA SUGGERITA
──────────────────┼──────────────────────────────────────┼─────────────────────
no_time           │ "Non ho tempo"                       │ → Usa script obiezione #1
                  │ "Sono molto impegnato"               │   dal DB (ladderOverrides)
                  │ "Richiamami tra un mese"             │
──────────────────┼──────────────────────────────────────┼─────────────────────
need_to_think     │ "Devo pensarci"                      │ → Usa script obiezione #2
                  │ "Ne parlo con il mio socio"          │   + ladder dei perché
                  │ "Ti faccio sapere"                   │
──────────────────┼──────────────────────────────────────┼─────────────────────
too_expensive     │ "Costa troppo"                       │ → Usa script obiezione #3
                  │ "Non ho budget"                      │   + reframe valore
                  │ "È fuori dalla mia portata"          │
──────────────────┼──────────────────────────────────────┼─────────────────────
not_interested    │ "Non mi interessa"                   │ → Ladder dei perché
                  │ "Non fa per me"                      │   per capire la vera
                  │ "Lascia stare"                       │   obiezione

2.4 Validazione Checkpoint
┌─────────────────────────────────────────────────────────────────────────────┐
│                     CHECKPOINT DISCOVERY - ESEMPIO                           │
└─────────────────────────────────────────────────────────────────────────────┘
FASE 3 - CHECKPOINT OBBLIGATORIO:
┌────────────────────────────────────────────┐
│ ✓ Hai capito lo STATO ATTUALE?             │ → Verificare nel transcript
│ ✓ Hai capito lo STATO DESIDERATO?          │ → Verificare nel transcript  
│ ✓ Hai identificato il BLOCCO principale?   │ → Verificare nel transcript
│ ✓ Hai fatto almeno 3 PERCHÉ?               │ → Contare ladder activations
└────────────────────────────────────────────┘
SE NON COMPLETO:
┌────────────────────────────────────────────┐
│ ⚠️ FEEDBACK: "Non puoi ancora avanzare     │
│    alla Fase 4. Ti manca: identificare     │
│    il blocco principale del prospect."     │
└────────────────────────────────────────────┘

2.5 Monitoraggio Tono (Anti-Robot)
┌─────────────────────────────────────────────────────────────────────────────┐
│                     CONTROLLI ANTI-ROBOT                                     │
└─────────────────────────────────────────────────────────────────────────────┘
PROBLEMA                          │ RILEVAMENTO                 │ CORREZIONE
──────────────────────────────────┼─────────────────────────────┼─────────────
Troppe domande consecutive        │ 3+ domande senza pause      │ "Fermati, 
                                  │                             │  commenta prima"
──────────────────────────────────┼─────────────────────────────┼─────────────
Messaggio troppo lungo            │ > 500 caratteri             │ "Sii più conciso,
                                  │                             │  una cosa alla volta"
──────────────────────────────────┼─────────────────────────────┼─────────────
Energy mismatch                   │ Fase richiede ALTO,         │ "Alza l'energia,
                                  │ AI sta usando BASSO         │  sii più entusiasta"
──────────────────────────────────┼─────────────────────────────┼─────────────
Non risponde alle domande         │ Prospect chiede, AI         │ "Rispondi prima
                                  │ continua con script         │  alla domanda!"

📦 COMPONENTE 3: FIX MESSAGGI UTENTE vs AI
Problema Attuale
┌─────────────────────────────────────────────────────────────────────────────┐
│                     PROBLEMA: CONFUSIONE MESSAGGI                            │
└─────────────────────────────────────────────────────────────────────────────┘
TIMELINE EVENTO:
───────────────────────────────────────────────────────────────────────────────
t=0   │ Prospect parla: "Fantastico!"
t=1   │ inputTranscription arriva (isFinal: false)  ← Chunk parziale
t=2   │ AI inizia a rispondere
t=3   │ outputTranscription arriva
t=4   │ turnComplete arriva  ← AI ha finito
t=5   │ FALLBACK: traccia pendingUserTranscript come messaggio UTENTE
      │ MA... potrebbe contenere eco dell'AI o testo parziale!
───────────────────────────────────────────────────────────────────────────────
RISULTATO: Il subagent vede "Fantastico!" come messaggio utente,
           ma era l'AI che lo ha detto!

Soluzione
// PRIMA (problematico) - gemini-live-ws-service.ts linea 2931
if (pendingUserTranscript.text.trim() && !pendingUserTranscript.hasFinalChunk) {
  await salesTracker.trackUserMessage(pendingUserTranscript.text.trim());
  // ❌ Traccia senza conferma che sia veramente dell'utente!
}
// DOPO (sicuro)
// 1. NON tracciare mai senza isFinal
// 2. Separare buffer per utente e AI
// 3. Validare che il messaggio non sia eco dell'AI
interface MessageBuffer {
  user: {
    text: string;
    confirmedAt?: Date;  // Solo se isFinal ricevuto
    isConfirmed: boolean;
  };
  ai: {
    text: string;
    completedAt?: Date;
  };
}
// Prima di passare al SalesManager:
if (!messageBuffer.user.isConfirmed) {
  console.log('⚠️ Skipping analysis: no confirmed user message');
  return;
}

📅 SEQUENZA IMPLEMENTAZIONE
┌─────────────────────────────────────────────────────────────────────────────┐
│                     ORDINE DI IMPLEMENTAZIONE                                │
└─────────────────────────────────────────────────────────────────────────────┘
SETTIMANA 1: Fix fondamentali
├── Task 1: Fix Script Manager (Opzione A)
│   ├── Modificare client-script-manager.tsx
│   ├── Modificare sales-scripts.ts (API)
│   └── Test salvataggio/caricamento ID
│
└── Task 7: Fix confusione messaggi
    ├── Separare buffer utente/AI
    ├── Rimuovere fallback problematico
    └── Validare messaggi prima di analisi
SETTIMANA 2: Sales Manager Agent base
├── Task 2: Architettura SalesManagerAgent
│   ├── Creare sales-manager-agent.ts
│   ├── Definire interfacce
│   └── Sostituire StepAdvancementAgent
│
└── Task 3: Rilevamento segnali acquisto
    ├── Pattern matching frasi
    ├── Integrazione con feedback injection
    └── Test con conversazioni reali
SETTIMANA 3: Funzionalità avanzate
├── Task 4: Gestione obiezioni
│   ├── Collegamento a script obiezioni DB
│   ├── Suggerimento risposte
│   └── Ladder dei perché automatico
│
├── Task 5: Validazione checkpoint
│   ├── Parsing checkpoint da script
│   ├── Verifica completamento
│   └── Blocco avanzamento se incompleto
│
└── Task 6: Monitoraggio tono
    ├── Contatori domande consecutive
    ├── Analisi lunghezza messaggi
    └── Verifica energy match

📁 FILE DA CREARE/MODIFICARE
File	Azione	Descrizione
server/ai/sales-manager-agent.ts	🆕 CREARE	Nuovo agente completo
server/ai/step-advancement-agent.ts	❌ DEPRECARE	Sostituito da SalesManager
server/ai/gemini-live-ws-service.ts	✏️ MODIFICARE	Integrare SalesManager + fix buffer
client/src/pages/client-script-manager.tsx	✏️ MODIFICARE	Usare structure come fonte verità
server/routes/sales-scripts.ts	✏️ MODIFICARE	Salvare structure JSON
shared/types/sales-manager.ts	🆕 CREARE	Tipi per SalesManager
✅ CRITERI DI SUCCESSO
Script Manager:

 Salvare e ricaricare script senza perdere energySettings
 Cancellare fase/step rimuove anche dati correlati
 Nessuna duplicazione al salvataggio
Sales Manager Agent:

 Rileva almeno 80% dei segnali di acquisto
 Rileva obiezioni e suggerisce risposta corretta
 Blocca avanzamento se checkpoint incompleto
 Corregge tono robotico in < 2 turni
Messaggi Utente/AI:

 Zero falsi positivi (AI tracciato come utente)
 Log chiari su chi ha detto cosa
