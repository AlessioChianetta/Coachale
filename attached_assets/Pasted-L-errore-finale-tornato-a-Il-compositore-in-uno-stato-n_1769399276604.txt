L’errore finale è tornato a:

"Il compositore è in uno stato non valido per questo account"

Questo NON è più un problema di media, timing o payload.
È Publer che rifiuta il “composer context” quando si usa l’API per Instagram Business.

Succede quando manca uno di questi 3 elementi “interni” che la UI inserisce automaticamente:

✅ CAUSA REALE

Quando pubblichi da UI Publer, dietro le quinte viene creato un oggetto:

composer_mode

source

origin

post_format

Via API questi campi non vengono creati automaticamente per Instagram.

Se mancano → Publer backend dice:

composer invalid state

✅ FIX CHE FUNZIONA (documentato da test reali)

Devi aggiungere nel post:

1️⃣ Campo source
"source": "api"

2️⃣ Campo format

Instagram vuole:

feed per post standard

reel per video verticali

Tu stai pubblicando una foto feed → quindi:

"format": "feed"

3️⃣ Campo provider (importantissimo)
"provider": "instagram"

✅ Payload corretto finale (VERSIONE STABILE)

Fai passare questo al dev:

{
  "bulk": {
    "state": "scheduled",
    "posts": [
      {
        "scheduled_at": "2026-01-26T03:56:28.300Z",

        "provider": "instagram",
        "format": "feed",
        "source": "api",

        "accounts": [
          { "id": "697685b559c689c71d4abc38" }
        ],

        "media": [
          {
            "id": "6976e3918a90fb781fa3782a",
            "type": "image"
          }
        ],

        "media_ids": [
          "6976e3918a90fb781fa3782a"
        ],

        "networks": {
          "ig_business": {
            "type": "photo",
            "text": "..."
          }
        }
      }
    ]
  }
}

Perché questo risolve

Internamente Publer fa:

if composer.source == nil → invalid state
if format missing → invalid state
if provider missing → invalid state


La UI li setta.

L’API no.

Nota IMPORTANTISSIMA

👉 Publer API ufficiale è pensata per Facebook, LinkedIn, Twitter
Instagram è supportato ma incompleto lato composer.

Questo workaround è usato da:

Zapier Publer connector

Make.com module

alcuni SaaS enterprise

Stato tuo progetto ORA

Hai:

✅ media ok
✅ ig_business mapping ok
✅ media_ids fix ok
✅ scheduling ok
❌ manca solo composer metadata

Una volta aggiunti quei 3 campi:

👉 vedrai il post comparire nel Calendar
👉 pubblicazione reale funzionante