# ===========================================
# ENVIRONMENT VARIABLES - Template
# ===========================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!
# ===========================================

# ============================================
# DATABASE CONFIGURATION (REQUIRED)
# ============================================
# PostgreSQL connection string
# 
# IMPORTANT: The app AUTO-DETECTS if you're using Supabase and automatically:
# - Enables SSL (sslmode=require)
# - Enables pgBouncer pooling
# - Optimizes connection pool settings
#
# For Supabase (RECOMMENDED):
#   Get from: Settings → Database → Connection String → "Transaction" mode
#   Example: postgresql://postgres.[PROJECT]:[PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
#   Note: Port 6543 = Transaction mode (recommended), Port 5432 = Session mode
#
# For standard PostgreSQL:
#   Local: postgresql://username:password@localhost:5432/database
#   Remote: postgresql://username:password@your-server:5432/database
#   Add SSL if needed: postgresql://username:password@host:5432/database?sslmode=require
#
DATABASE_URL=postgresql://username:password@host:5432/database

# ============================================
# SECURITY (REQUIRED)
# ============================================
# Secret key for Express session encryption
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_random_session_secret_here_min_32_chars

# Encryption key for sensitive data in database (e.g., passwords)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your_random_encryption_key_here_64_chars

# ============================================
# PERCORSO CAPITALE INTEGRATION (OPTIONAL)
# ============================================
# API credentials for Percorso Capitale financial data integration
# Leave empty if you don't use this integration
PERCORSO_CAPITALE_API_KEY=pc_master_your_api_key_here
PERCORSO_CAPITALE_BASE_URL=https://api.percorsocapitale.it

# ============================================
# EMAIL SCHEDULER (OPTIONAL)
# ============================================
# Cron expression for automated email sending
# Default: */5 * * * * (every 5 minutes)
# Disable: set EMAIL_SCHEDULER_ENABLED=false
EMAIL_SCHEDULER_CRON=*/5 * * * *

# ============================================
# FEATURE FLAGS (OPTIONAL)
# ============================================
# Enable/disable features (default: enabled if not set)
# EMAIL_SCHEDULER_ENABLED=true
# EMAIL_SCHEDULER_RUN_ON_STARTUP=false
# WHATSAPP_POLLING_ENABLED=true
# CALENDAR_SYNC_ENABLED=true
# PROACTIVE_OUTREACH_ENABLED=true
# LEAD_POLLING_ENABLED=true
# TRAINING_AGGREGATOR_ENABLED=true

# ============================================
# RUNTIME CONFIGURATION (OPTIONAL)
# ============================================
# Server port (default: 5000)
# PORT=5000

# Node environment (development/production)
# NODE_ENV=development
